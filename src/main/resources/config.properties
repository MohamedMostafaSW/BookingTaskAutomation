# Application Configuration File

# Base URL
base.url=www.booking.com

# Browser Configuration
browser=chrome
headless=false
maximize=true

# Timeouts (in seconds)
implicit.wait=10
page.load.timeout=30
